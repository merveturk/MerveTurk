import re
text=open("first.txt")
l=list()
for line in text:
    y=re.findall("[0-9]+",line)
    if not y == []:
        for i in y:
            l.append(i)
count=0
for j in l:
    count=count+int(j)

print(count)
